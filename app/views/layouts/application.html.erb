<!DOCTYPE html>
<html>
  <head>
    <title> <%= content_for?(:title) ? yield(:title) : 'Tai Chi at UCT' %> | UCT Martial Arts</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name='description' content='<%= yield_meta_tag(:description, 'UCT Martial Arts | Tai Chi') %>' />
    <meta name='keywords' content='<%= yield_meta_tag(:keywords, 'relax,breathing,UCT sports, anxiety, balance, cape town') %>' />

    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if current_page?(root_path)%>  
      <div class="h-01">
        <%= render 'layouts/header' %>
        <div class="container">
          <div class="row">
            <%= render 'layouts/flashy' %>
          </div>
          <div class="row pt-2"><!-- for mobile -->
            <div class="col-sm-5 col-md-6 col-lg-6 d-block d-sm-none">
              <div class="container">
                <div class="text-center">
                  <h1>header 1</h1>
                  <p>strong catchy message here</p>
                </div>
              </div>
            </div>
            <!-- bigger screens -->
            <div class="col-sm-5 col-md-6 col-lg-6 d-none d-sm-block">
              <h1>
                Sit, <br/>
                Relax, <br/>
                Breathe
              </h1>
              <p class="py-4">
                lorem ipsum lol
              </p>
              <%= link_to "Read More", about_path, class: "btn btn-secondary"%>
            </div>
            <div class="col-sm-5 col-md-6 col-lg-6 d-none d-sm-block py-4">
            </div>
          </div>
        </div>
      </div>
      <%= yield %>
      <%= render 'layouts/footer' %>
    <% else %>
      <%= render 'layouts/header' %>
      <%= render 'layouts/flashy' %>
      <%= yield %>
      <%= render 'layouts/footer' %>
    <% end %>
  </body>
</html>
